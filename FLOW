index   (Web UI)
    common
        config.*
        Filters
        Db
        Log
        Lang(Default)
        Updater
        Appctx
    usermode
        Web
            Frotend (init)
        Lang (User)
        User
        Frontend (included)

monnet_cli (Do CLI tasks)
    climode
        config (default)
        common
            - *
    cron jobs
        ping_known_host

monnet-discovery (Scan networks)
    climode
        - *
    ping_nets

refresher (refresh web data)
    common
        - *
    usermode
        - *

submitter (submit web data)
    common
        - *
    usermode
        -
feedme  (submit agents data)
    common
        - *
    feedme.inc